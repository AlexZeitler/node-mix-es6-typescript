# Mixing ES6 and TypeScript in Node.js
Sample repo to show mixing of ES6 and TypeScript in Node.js

## Running the sample
```bash
npm install -g typescript
npm install
tsc
node build/b.js
```

Expected output:

```bash
Hello World
```